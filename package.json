{
  "name": "adl-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "cf:build": "opennextjs-cloudflare build",
    "cf:preview": "npm run cf:build && opennextjs-cloudflare preview",
    "cf:deploy": "npm run cf:build && opennextjs-cloudflare deploy",
    "cf:upload": "npm run cf:build && opennextjs-cloudflare upload",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "next lint --fix --file",
      "bash -c 'tsc --noEmit'"
    ],
    "*.{js,jsx,json,css,md}": [
      "next lint --fix --file"
    ]
  },
  "//overrides": [
    "Both are build-time-only transitives pinned by Next; neither ships in the",
    "Worker bundle. `npm audit fix` could only clear them by downgrading Next to",
    "9.3.3, so they are pinned forward here instead.",
    "sharp   <0.35.0 — inherited libvips CVEs (GHSA-f88m-g3jw-g9cj)",
    "postcss <8.5.10 — XSS via unescaped </style> in stringify output",
    "Drop an entry once the upstream package requires the patched range itself.",
    "",
    "NOT overridable — brace-expansion <=5.0.7 (GHSA-mh99-v99m-4gvg, DoS via",
    "unbounded expansion). The only patched release is 5.0.8, which removed the",
    "default export; minimatch's ESM build does `import expand from` and dies with",
    "'does not provide an export named default'. `next build` still passes because",
    "it never loads that path — `npm run cf:build` (what CI runs) fails. Verified",
    "2026-07-25. It is reached only via minimatch/glob in the eslint + opennextjs",
    "build chain, where the input is this repo's own file paths, so it is not a",
    "runtime risk. Wait for minimatch to publish a build compatible with 5.0.8."
  ],
  "overrides": {
    "sharp": "^0.35.2",
    "postcss": "$postcss"
  },
  "dependencies": {
    "chart.js": "^4.4.9",
    "clsx": "^2.1.1",
    "deepmerge-ts": "^7.1.5",
    "geist": "^1.7.2",
    "mongodb": "^6.10.0",
    "next": "^15.2.4",
    "react": "^19.1.0",
    "react-chartjs-2": "^5.3.0",
    "react-dom": "^19.1.0",
    "react-intersection-observer": "^9.16.0",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@opennextjs/cloudflare": "^1.0.0",
    "@tailwindcss/postcss": "^4.1.8",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.21",
    "eslint": "^9",
    "eslint-config-next": "15.2.4",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "postcss": "^8.5.23",
    "tailwindcss": "^4.1.4",
    "typescript": "^5",
    "wrangler": "^4.0.0"
  }
}
